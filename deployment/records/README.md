# Deployment records

Human-readable records for real Juno deployment outputs. These files are public summaries; raw tx JSON and wasm binaries should stay in operator archives unless a release process explicitly publishes them.
